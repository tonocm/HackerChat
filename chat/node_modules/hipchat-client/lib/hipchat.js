// Generated by CoffeeScript 1.7.1
(function() {
  var HipChat, Rooms, Users;

  Rooms = require('./api/rooms');

  Users = require('./api/users');

  HipChat = (function() {
    HipChat.prototype.host = 'https://api.hipchat.com';

    HipChat.prototype.api = [];

    function HipChat(apiKey) {
      this.apiKey = apiKey;
      this.addResources({
        'rooms': Rooms,
        'users': Users
      });
    }

    HipChat.prototype.addResources = function(resources) {
      var module, name, _results;
      _results = [];
      for (name in resources) {
        module = resources[name];
        _results.push(this.api[name] = new module(this));
      }
      return _results;
    };

    return HipChat;

  })();

  module.exports = HipChat;

}).call(this);
